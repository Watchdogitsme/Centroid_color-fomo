# Centroid_color-fomo
detects the color and is built with fomo so it can run on small size computers like esp32 

to run on windows 
pip install torch torchvision opencv-python numpy

python live_feed.py

to run on linux
change 
cv2.CAP_DSHOW to cv2.CAP_V4L2 and then do the sam thing as windows


if any issues pls ask any ai llm 
